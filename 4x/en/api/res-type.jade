section
  h3(id='res.type') res.type(type)
  
  p.
    Content-Type을 <code>type</code>로 설정합니다.

  +js.
    res.type('.html');
    res.type('html');
    res.type('json');
    res.type('application/json');
    res.type('png');
