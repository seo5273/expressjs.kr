section
  h3(id='req.get') req.get(field)
  p 
    | 대소문자를 구분하지 않는 request 헤더값을 불러옵니다.
    | <em>Referrer</em>과 <em>Referer</em>은 서로 이름이 틀리더라도 사용이 가능합니다.

  +js.
    req.get('Content-Type');
    // => "text/plain"
  
    req.get('content-type');
    // => "text/plain"
  
    req.get('Something');
    // => undefined

  p <code>req.header(field)</code>로도 사용할 수 있습니다.